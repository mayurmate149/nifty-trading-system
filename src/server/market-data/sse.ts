/**
 * Market Data — SSE (Server-Sent Events)
 *
 * Streams live market ticks to the frontend with ≤500ms latency.
 */

// TODO: Phase 3
// Create a Next.js route handler that:
// 1. Opens a readable stream
// 2. Subscribes to tick updates from the WS client
// 3. Pushes SSE events to the client
// 4. Handles client disconnect / cleanup

export function createSSEStream() {
  throw new Error("Not implemented — Phase 3");
}
